# 依赖（阶段2：OCR/微信发送）
fastapi>=0.110
uvicorn>=0.29
openai>=1.0          # LLM 接入（OpenAI 兼容/DeepSeek）
pystray>=0.19        # 桌面托盘
Pillow>=10.0         # 托盘图标/截图
uiautomation>=2.0    # 微信 UIA 采集与发送
pywinauto>=0.6       # 备用 UI 自动化
python-pptx>=0.6.21  # 汇报材料转 PPT
rapidocr-onnxruntime>=1.3  # OCR 识别（Windows OCR 不支持 py3.13 时用）
faster-whisper>=1.0  # 本地录音转文字（可选加载）
